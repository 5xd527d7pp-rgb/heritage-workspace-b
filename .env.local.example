# ローカル開発用。コピー: cp .env.local.example .env.local

# Claude API（/api/ai）。未設定時はスタブ応答。
ANTHROPIC_API_KEY=sk-ant-api03-...

# Neon Postgres（/api/workspace）。未設定時は localStorage のみ。
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
